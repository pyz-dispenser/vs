[project]
name = "vs"
version = "0.0.1"
description = "vs environment tool"
authors = [
    {name = "Jamie Bliss",email = "jamie@ivyleav.es"}
]
license = {text = "GPL-3.0"}
readme = "README.md"
requires-python = ">=3.9,<4.0"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
